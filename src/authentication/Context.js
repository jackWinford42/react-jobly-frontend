import React from "react";

/** The one context instance in the front-end. Context is created to hold
 * information or callback functions that many components in the app
 * will use.
 */

const Context = React.createContext();

export default Context;